name: "CodeQL Advanced"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '27 6 * * 3'

permissions:
  security-events: write
  contents: read
  packages: read
  actions: read

jobs:
  codeql:
    uses: reactiveui/actions-common/.github/workflows/workflow-common-codeql.yml@b285525c4af878b0fe3fbb00d9f2ae862e402395 # main
    with:
      srcFolder: src
      solutionFile: Splat.DI.SourceGenerator.slnx
      installWorkloads: false
      analyzeCSharp: true
      analyzeActions: true
      analyzeJavaScript: false
